import java.util.*;
import java.io.*;

class Main {
    public static void main() {
        Scanner sc = new Scanner(System.in);
        int n = Integer.parseInt(sc.next());
        int k = Integer.parseInt(sc.next());

        // 무한한 재귀 함수를 구현해본다.
        // 0번 회차에서, 이전에 1번 동전을 사용한 경우, 2번 동전을 사용한 경우, 3번 동전을 사용한 경우를 모두 고려해서
        // max값을 취한다.
        // 1번 동전을 사용한 경우 + 지금 1번을 선택한 경우
        // 2번 동전을 사용한 경우 + 지금 2번을 선택한 경우
    }
}